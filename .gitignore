.DS_Store
node_modules/
gen.log

# Xcode / build artifacts (Capacitor's ios/.gitignore covers ios/App/*)
legacy-ios-shell/build/
*.xcuserdatad/
DerivedData/

# local-only Claude settings (launch.json is shared)
.claude/settings.local.json

# Firebase config (keep out of the public repo; lives on disk for builds)
GoogleService-Info.plist

# Xcode archives / export output
build/
